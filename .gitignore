.DS_Store
node_modules/
.env
.env.*
*.log
logs/
dist/
coverage/
.codex/
# 防御性忽略:Synod 运行/开发都不需要项目级 .mcp.json
# (agent-bridge 作为 Claude Code 插件自带 MCP 工具,见 README);此处仅防止误提交本地实验配置
.mcp.json

# Flow engine runtime artifacts (generated at project root)
/run.log.jsonl
/artifacts/
