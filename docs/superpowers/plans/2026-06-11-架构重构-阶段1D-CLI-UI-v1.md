# 架构重构(阶段 1D · CLI UI v1)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 本仓也可沿用三方闭环(deepseek 开发 / codex 只读审+实跑 / Claude Code 验收,见 `docs/HANDOFF.md`)。

**Goal:** 把 REPL 主持人模式做到**易用看得清**——零三方依赖(纯 ANSI + `node:readline`)地补齐:per-label 着色多路输出、turn 边界线、转发可视化、模式徽标提示符、tab 补全、历史持久化、`/help` 分组、`/close`/`/status`、`/sessions` 表格、flow 运行视图头尾横幅,以及 config 通知钩子(onDone/onError/onApprovalNeeded)+ 终端铃。这是 V1「三使用面合一内核」里**主持人面**与 **workflow 面**的交互层收尾(1D),严守「主持人模式零回归」。

**Architecture:** 新增薄 UI 层 `src/ui/*`(全为纯函数模块,可单测):`ansi.mjs`(`color/enabled/labelColor/stripAnsi`,所有 UI 出字单点)→ `decorations.mjs`(turn 边界线/relay 横幅渲染)、`prompt.mjs`(模式徽标)、`help.mjs`(分组帮助)、`sessions-table.mjs`(表格)、`completer.mjs`(tab 补全)、`history.mjs`(历史)、`flow-view.mjs`(flow 头尾横幅+计数)。通知钩子落 `src/notify.mjs`(命令钩子 + BEL/OSC0)。内核接线点最小化:着色在 `createLineBuffer` 的 `[label]` 前缀单点、turn 边界线在 session-manager 的 `status:idle` 钩子、relay 横幅在 cli 的 relay enqueue 回调、`/close` 串起 `sm.close + registry.removeForLabel + wireControl.dropLabel`(P2-19 + P2-25)。**所有视觉装饰(着色/边界线/relay 横幅/徽标)一律 TTY 门控(`enabled(stdout)`)**,非 TTY 逐字节维持现状——这是「主持人模式零回归」与现有 e2e 不受影响的根本保证。

**Tech Stack:** Node 20+ 零三方依赖;`node --test` + `test/helpers/fake-backend.mjs`;e2e 用 `fixtures/backends/echo-cli.mjs`(1A/1B 已存在的假外部 CLI)充当后端,零真 agent。

---

## Scope 与基线

**Spec:** `docs/CLI_UI_DESIGN.md` §1–§5、§9 标 **v1** 的全部条目;`docs/V1_REVIEW_AND_ARCHITECTURE.md` §4.8(CLI 形态:`/close`、stdin 单一所有权前提)、§4.13(通知钩子 + 终端铃)、§2.3 的 **P2-19**(`/close <label>` + relay 解绑)与 **P2-25**(`/close` 时清 control-wire `_depthMap`);`docs/V1.md` 看板「阶段 1D」「复审 R2 → 1D 覆盖」。

**v1 项 → 本计划 Task 的映射在文末 Self-Review 节给出。**

**本计划不含**(显式留给后续期):
- **CLI_UI_DESIGN 标 v2 / 后续期的项**(逐条列名,不实现):
  - §6 **team 视图全部**(`/team`、team status、member 角色名 label、`⚙` 编排行)— v2/阶段 2。
  - §5 **approve 反色提问块 + REPL 输入让位**(`┃ approve?`)— 阶段 1C(依赖 P1-8 InputRouter)。
  - §5 **flow 内 Ctrl-C 中断不退进程**— 阶段 1C(依赖 ctx AbortSignal)。
  - §5 flow 尾行 **`log: ~/.synod/runs/<runId>/`** — 依赖 1C 的 per-run 目录;本计划 flow-view 尾行**不含** run 目录路径(只给 turns/sessions/耗时)。
  - §7 **`--json` 结构化输出**、**`--no-color` 命令行 flag**、**退出码表写进 `--help`**— 阶段 3。
  - §3.1 **`/team` 名补全**— v2;**`/flow` 坏 flow 显示 `name (error)`** 依赖 1C-a 的韧性 `discoverFlows`,本计划只补全 `discoverFlows` 返回的有效 flow 名。
  - §3.4 历史**敏感词过滤**— 设计明示不做。
- **退出码 5(awaiting human)/headless 存断点退出**— 1C-b/阶段 3(本计划只落「通知钩子 + 铃」,不碰退出码 5)。
- **`/runs`、`/abort`、`/team` 命令本体**— 1C/阶段 2。

> 注:**`NO_COLOR` 环境变量**虽在 §9 与 `--json` 同列阶段 3,但它是 `enabled()` 的基础门控(§8.1 明定 `enabled = isTTY && !NO_COLOR`),属本计划地基,**v1 即生效**;仅 `--no-color` 命令行 flag 推迟阶段 3。

**基线依赖(1A/1B 已落地,本计划直接引用,不得改其契约):**
- `src/backends/registry.mjs` 的 `backendNames()`(tab 补全 `--agent` 后接 backend 名的来源)。
- `src/config.mjs` 的 `loadConfig()`(返回 `{agents,backends,defaults,sources}`,本计划追加 `hooks` 段)与 `registerConfigBackends()`;`config.agents` 是 `/open +profile` 与补全 `+profile` 的来源。
- `src/session-manager.mjs` 的 `createSessionManager/createLineBuffer/checkAgentAvailable`、`sm.open/enqueue/use/list/drainAll/flushAll/closeAll/entries/_sessions/currentLabel`。
- `src/relay.mjs` 的 `createRelayRegistry`(已有 `removeForLabel`)、`src/control-wire.mjs` 的 `wireControl`(已有 `_depthMap`)。
- `src/flow.mjs` 的 `createDefaultProgressSink`(进度 sink,本计划用 `countingSink` 包它)、`main`(REPL `/flow` 经 `--progress` 调用)。
- `fixtures/backends/echo-cli.mjs`(1A/1B 已建,e2e 用)。

**与 1C-a InputRouter 的关系(写清,避免误依赖):** 1C-a(同日另出)会引入进程内唯一 readline 的 `createInputRouter({stdin,stdout})`,其 `router.rl` 为唯一 `readline.Interface`。**本计划 1D 不依赖 1C-a**:tab 补全与历史直接挂在**当前 `cli.mjs` 的 `createRepl()` 自建 readline** 上(开工检查会断言 `createRepl` 仍在 cli.mjs)。两计划并行;若 1C-a 先落地并以 `router.rl` 取代 `createRepl` 的 readline,则把本计划在 `createRepl` 上加的 `completer`/`history`/`onSubmit` 接到 `router.rl` 的构造选项即可(接线点同形,不改 UI 模块)。本计划**只需 readline 本体**,不重建第二个 readline。

**分支建议:** `cli-ui-v1`(从含 1A/1B 的 main 切出)。

**硬约束(违者打回):**
1. **主持人模式零回归**:`/open /use /sessions @label @all /relay /unrelay /relays /flow /exit` 行为不变(仅**增**命令 `/close /status /help`,**增**装饰);现有 `npm run test:e2e` A1–A8(+E1–4)57 必全过;每 Task `npm test` 全绿才 commit。
2. **非 TTY / NO_COLOR 逐字节降级**:`stdout.isTTY` 假 或 `NO_COLOR` 置位时,**零 ANSI、零装饰行、提示符恒为 `"> "`**(不破 e2e 的 `"> "` 探测)。一切装饰只在 `enabled(stdout,env)` 为真时输出。
3. **零第三方依赖**:无 chalk/ink;ANSI 手写自表(不用 `util.inspect.colors`);所有 UI 出字过 `src/ui/ansi.mjs` 单点,禁裸写 `\x1b[`(渲染模块内除外)。
4. **headless-safe**:通知钩子(onDone/onError/onApprovalNeeded)在非 TTY/headless **同样触发**(它们是「无人值守」语义);BEL/OSC0 **仅 TTY** 触发(非 TTY 不写控制字符)。
5. **Windows 兼容横切**(§7 拍板):着色/BEL/OSC0 的 win32 行为写明——现代 Windows Terminal/Win10+ conhost 支持 VT,Node 自动开 VT 处理;`enabled()` 统一以 `isTTY && !NO_COLOR` 门控,旧 conhost 即便不解析 OSC0 也只是**静默吞掉**该序列(BEL 全平台可响),不产生可见乱码;非 TTY(CI/管道,含 Windows 管道)零序列。每阶段 DoD 含用户 Windows 实测一轮(看板「阶段 1 总验收」)。

## File Structure

```
src/ui/ansi.mjs            新增 — color/enabled/labelColor/stripAnsi(零依赖手写 ANSI;UI 出字单点)
src/ui/decorations.mjs     新增 — turnBoundary/relayBanner 纯渲染(§2.2/§2.3)
src/ui/prompt.mjs          新增 — renderPrompt 模式徽标 + 忙闲(§1)
src/ui/help.mjs            新增 — HELP_TEXT 分组 + helpForCommand(§3.2)
src/ui/sessions-table.mjs  新增 — renderSessionsTable(/sessions 表格 + relay 列,§4)
src/ui/completer.mjs       新增 — makeCompleter(tab 补全纯函数,§3.1)
src/ui/history.mjs         新增 — loadHistory/appendHistory/historyPath(~/.synod/history,§3.4)
src/ui/flow-view.mjs       新增 — createFlowView(flow 运行视图头尾横幅 + 计数,§5)
src/notify.mjs             新增 — createNotifier(命令钩子 + BEL/OSC0,§4.13)
src/session-manager.mjs    修改 — 着色 lineBuffer + turn 边界线 + close(label) + 表格 list + No session hint + relays 注入
src/repl-dispatch.mjs      修改 — /close /status /help + onCloseLabel/flowStatus 依赖 + /relay hint
src/control-wire.mjs       修改 — 暴露 dropLabel(清 _depthMap,P2-25)
src/relay.mjs              修改 — onTurnComplete 透传 {from,chars} meta(§2.3 可视化用)
src/cli.mjs                修改 — 动态提示符 + completer/history 接线 + relay 横幅 + dropLabel 接线 + flowStatus
src/config.mjs             修改 — hooks 段校验 + 合并
src/flow.mjs               修改 — flow 运行视图接线 + onDone/onError 钩子
src/flow/runtime.mjs       修改 — onApprovalNeeded 透传给 approve
src/flow/api/approve.mjs   修改 — onApprovalNeeded 触发点
test/ui.ansi.test.mjs / ui.line-color.test.mjs / ui.decorations.test.mjs / ui.prompt.test.mjs   新增
test/ui.help-hint.test.mjs / ui.close-status.test.mjs / ui.sessions-table.test.mjs              新增
test/ui.completer.test.mjs / ui.history.test.mjs / ui.flow-view.test.mjs                        新增
test/notify.test.mjs / cli-ui.integration.test.mjs                                              新增
```

**开工检查(看板第一条):**

- [ ] `git status` 干净;`npm test` 全绿(基线 **653**);`git log --oneline -3` 在 main 含 1A/1B(`f30b5ff`/`3cf1e7f`/`9bbeb51`)。新建分支 `cli-ui-v1`。
- [ ] 符号存在性断言(开工口径):
  ```bash
  git grep -n "function createRepl" src/cli.mjs          # 期望命中:1D 锚定它的 readline
  git grep -n "createDefaultProgressSink" src/flow.mjs   # 期望命中:flow-view 包它
  git grep -n "removeForLabel" src/relay.mjs             # 期望命中:/close 复用
  git grep -n "_depthMap" src/control-wire.mjs           # 期望命中:dropLabel 清它(P2-25)
  git grep -n "backendNames" src/backends/registry.mjs   # 期望命中:补全 --agent 用
  ```
  Expected: 全部各 ≥1 命中。任一缺失 → 停,回看基线是否真为 1A/1B 之后。

---

### Task 1: `src/ui/ansi.mjs` — 零依赖 ANSI 工具(UI 出字单点)

**Files:**
- Create: `src/ui/ansi.mjs`
- Test: `test/ui.ansi.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/ui.ansi.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { enabled, color, labelColor, stripAnsi, PALETTE_CODES } from "../src/ui/ansi.mjs";

test("color:SGR 包裹 + reset", () => {
  assert.equal(color(36, "hi"), "\x1b[36mhi\x1b[0m");
});

test("enabled:isTTY && !NO_COLOR(§8.1)", () => {
  assert.equal(enabled({ isTTY: true }, {}), true);
  assert.equal(enabled({ isTTY: false }, {}), false);
  assert.equal(enabled(undefined, {}), false);
  assert.equal(enabled({ isTTY: true }, { NO_COLOR: "1" }), false);
  assert.equal(enabled({ isTTY: true }, { NO_COLOR: "" }), true, "空串视为未设置(§8.1 的 !NO_COLOR)");
});

test("labelColor:同 label 恒定且落在 8 色调色板内", () => {
  const c = labelColor("omp#1");
  assert.ok(PALETTE_CODES.includes(c));
  assert.equal(labelColor("omp#1"), c, "同一 label 永远同色");
  assert.equal(PALETTE_CODES.length, 8, "8 色循环(§2.1)");
});

test("stripAnsi:还原纯文本(§8.5 strip 等价基座)", () => {
  assert.equal(stripAnsi(color(labelColor("omp#1"), "[omp#1]")), "[omp#1]");
  assert.equal(stripAnsi("no codes"), "no codes");
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.ansi.test.mjs`
Expected: FAIL — `Cannot find module '../src/ui/ansi.mjs'`

- [ ] **Step 3: 最小实现**

```js
// synod/src/ui/ansi.mjs — 零依赖手写 ANSI。所有 UI 模块经此出字,禁裸写 \x1b[。
//
// 门控语义(§8.1 / 硬约束 2):enabled(stream, env) = stream.isTTY && !NO_COLOR。
// 非 TTY / 管道 / CI / NO_COLOR → false → 调用方一律走纯文本路径,零序列。
// Windows:Node 在 Win10+ / Windows Terminal 自动启用 VT 处理,isTTY 为真即可着色;
// 旧 conhost 不解析也只静默吞掉 SGR,非 TTY(含 Windows 管道)则根本不出序列。

// 8 色调色板(§2.1):绿/黄/蓝/品红/青/亮绿/亮黄/亮品红。
const PALETTE = [32, 33, 34, 35, 36, 92, 93, 95];

/** stream 可着色?TTY 且未设 NO_COLOR(空串视为未设)。 */
export function enabled(stream, env = process.env) {
  return Boolean(stream && stream.isTTY) && !env.NO_COLOR;
}

/** 用 SGR code 包裹 s 并 reset。调用前自行判 enabled()。 */
export function color(code, s) {
  return `\x1b[${code}m${s}\x1b[0m`;
}

/** label → 固定 SGR code(同 label 永远同色,8 色循环)。 */
export function labelColor(label) {
  let h = 0;
  for (let i = 0; i < label.length; i += 1) {
    h = (Math.imul(h, 31) + label.charCodeAt(i)) >>> 0;
  }
  return PALETTE[h % PALETTE.length];
}

const ANSI_RE = /\x1b\[[0-9;]*m/g;
/** 去除 SGR 序列(测试断言「strip 后等于纯文本路径」用)。 */
export function stripAnsi(s) {
  return String(s).replace(ANSI_RE, "");
}

export const PALETTE_CODES = PALETTE;
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.ansi.test.mjs && npm test`
Expected: 全部 PASS(基线 653 + 4)

- [ ] **Step 5: Commit**

```bash
git add src/ui/ansi.mjs test/ui.ansi.test.mjs
git commit -m "feat(ui): 零依赖 ANSI 工具(color/enabled/labelColor/stripAnsi,出字单点)"
```

---

### Task 2: 着色多路输出 — `createLineBuffer` per-label 着色

**Files:**
- Modify: `src/session-manager.mjs`(`createLineBuffer` 加 `colorize` 选项;`open()` 据 `enabled(stdout)` 着色)
- Test: `test/ui.line-color.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/ui.line-color.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { createLineBuffer, createSessionManager } from "../src/session-manager.mjs";
import { color, labelColor, stripAnsi } from "../src/ui/ansi.mjs";
import { FakeSession } from "./helpers/fake-backend.mjs";

test("createLineBuffer:colorize 着色 [label] 前缀,strip 后逐字节等于纯文本", () => {
  const lines = [];
  const stdout = { write: (s) => lines.push(s) };
  const colorize = (s) => color(labelColor("omp#1"), s);
  const buf = createLineBuffer("omp#1", stdout, { colorize });
  buf.feed("hello\n");
  assert.equal(lines.length, 1);
  assert.notEqual(lines[0], "[omp#1] hello\n", "TTY 路径含 ANSI");
  assert.equal(stripAnsi(lines[0]), "[omp#1] hello\n", "strip 后等于纯文本路径");
});

test("createLineBuffer:无 colorize → 纯文本(非 TTY 零 ANSI,现状不变)", () => {
  const lines = [];
  const stdout = { write: (s) => lines.push(s) };
  const buf = createLineBuffer("omp#1", stdout);
  buf.feed("hi\n");
  buf.flush();
  assert.deepEqual(lines, ["[omp#1] hi\n"]);
});

test("createSessionManager:stdout.isTTY → delta 行着色;非 TTY → 零 ANSI", async () => {
  async function run(isTTY) {
    const lines = [];
    const stdout = { isTTY, write: (s) => lines.push(s) };
    const sm = createSessionManager({
      openBackend: async () => new FakeSession({ deltas: ["yo\n"] }),
      stdout, stderr: { write() {} }, report: { omp: { available: true } },
      cwd: process.cwd(), defaults: {}, onIdle: () => {},
    });
    const label = await sm.open({ agent: "omp" });
    await sm.enqueue({ target: label, msg: "go" });
    await sm.drainAll(); sm.flushAll(); sm.closeAll();
    return lines.join("").split("\n").find((l) => l.includes("yo")) ?? "";
  }
  const tty = await run(true);
  const plain = await run(false);
  assert.ok(/\x1b\[/.test(tty), "TTY 的 delta 行含 ANSI");
  assert.ok(!/\x1b\[/.test(plain), "非 TTY 的 delta 行零 ANSI");
  assert.equal(stripAnsi(tty), plain, "strip(TTY delta 行)=非 TTY delta 行");
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.line-color.test.mjs`
Expected: FAIL — `createLineBuffer` 忽略 `colorize`(输出无 ANSI);第三测 `tty` 不含 `\x1b`

- [ ] **Step 3: 实现**

① `src/session-manager.mjs` 头部(第 6 行注释下)加 import:

```js
import { enabled, color, labelColor } from "./ui/ansi.mjs";
```

② `createLineBuffer`(:8-26)替换为(加 `colorize` 选项,前缀只着色一次):

```js
function createLineBuffer(label, stdout = process.stdout, { colorize } = {}) {
  const prefix = colorize ? colorize(`[${label}]`) : `[${label}]`;
  let buf = "";
  return {
    feed(chunk) {
      buf += chunk;
      const lines = buf.split("\n");
      buf = /** @type {string} */ (lines.pop());
      for (const line of lines) {
        stdout.write(`${prefix} ${line}\n`);
      }
    },
    flush() {
      if (buf.length > 0) {
        stdout.write(`${prefix} ${buf}\n`);
        buf = "";
      }
    },
  };
}
```

③ `open()` 里创建 lineBuf 处(:148)替换为(着色门控算一次,Task 3 的 turn 边界线复用 `useColor`):

```js
    const useColor = enabled(stdout);
    const colorize = useColor ? (s) => color(labelColor(label), s) : null;
    const lineBuf = createLineBuffer(label, stdout, { colorize });
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.line-color.test.mjs && npm test`
Expected: 全部 PASS(既有 `test/line-buffer.test.mjs`、`test/session-manager.test.mjs` 不回归——它们的 fake stdout 无 `isTTY` → `enabled` 假 → 输出逐字节同旧)

- [ ] **Step 5: Commit**

```bash
git add src/session-manager.mjs test/ui.line-color.test.mjs
git commit -m "feat(ui): per-label 着色多路输出([label] 前缀,TTY 门控降级)"
```

---

### Task 3: turn 边界线(§2.2)+ relay 可视化(§2.3)

**Files:**
- Create: `src/ui/decorations.mjs`
- Modify: `src/session-manager.mjs`(`open()` 的 `status` 钩子写 turn 边界线)
- Modify: `src/relay.mjs`(`onTurnComplete` 透传 `{from,chars}` meta)
- Modify: `src/cli.mjs`(relay enqueue 回调写横幅)
- Test: `test/ui.decorations.test.mjs`

> **门控决定(设计冲突的处置,务必照此):** CLI_UI_DESIGN §2.2/§2.3 写「非 TTY:纯文本一行」,但 §8.5 同篇承诺「现有 acceptance 不受影响」、硬约束 1 要求 A1–A8 零回归。二者冲突。**裁定:turn 边界线与 relay 横幅一律 TTY 门控(`enabled(stdout)`),非 TTY 不输出任何新行。** 理由:边界线/横幅是纯装饰,非 TTY(管道/CI/`--task`)本就应零噪音(§0.2 精神),且这是唯一能逐字节守住 e2e 的方案。已在文末 Self-Review 记为有意偏离。

- [ ] **Step 1: 写失败测试**

```js
// test/ui.decorations.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { turnBoundary, relayBanner } from "../src/ui/decorations.mjs";
import { stripAnsi } from "../src/ui/ansi.mjs";
import { createRelayRegistry } from "../src/relay.mjs";
import { createSessionManager } from "../src/session-manager.mjs";
import { FakeSession } from "./helpers/fake-backend.mjs";

test("turnBoundary:[label] ── done · Xs ── 形态 + 着色,strip 后纯文本", () => {
  const s = turnBoundary("omp#1", "12.3");
  assert.ok(/\x1b\[/.test(s), "TTY 渲染含 ANSI");
  assert.equal(stripAnsi(s), "[omp#1] ── done · 12.3s ──────────\n");
});

test("relayBanner:[to] ◀─ relay from <from> (Nk chars) + 着色", () => {
  assert.equal(stripAnsi(relayBanner("codex#1", "omp#1", 1234)), "[codex#1] ◀─ relay from omp#1 (1.2k chars)\n");
  assert.equal(stripAnsi(relayBanner("codex#1", "omp#1", 42)), "[codex#1] ◀─ relay from omp#1 (42 chars)\n");
});

test("relay.onTurnComplete 透传 {from,chars} meta 给 enqueue 回调(§2.3)", () => {
  const calls = [];
  const reg = createRelayRegistry((to, msg, meta) => calls.push({ to, msg, meta }));
  reg.add("omp#1", "codex#1");
  reg.onTurnComplete("omp#1", "hello world");
  assert.equal(calls[0].to, "codex#1");
  assert.match(calls[0].msg, /\[relay from omp#1\]/);
  assert.deepEqual(calls[0].meta, { from: "omp#1", chars: 11 });
});

test("session-manager:TTY turn 完成写边界线;非 TTY 无", async () => {
  async function run(isTTY) {
    const lines = [];
    const sm = createSessionManager({
      openBackend: async () => new FakeSession({ deltas: ["x\n"] }),
      stdout: { isTTY, write: (s) => lines.push(s) }, stderr: { write() {} },
      report: { omp: { available: true } }, cwd: process.cwd(),
      defaults: {}, onIdle: () => {},
    });
    const label = await sm.open({ agent: "omp" });
    await sm.enqueue({ target: label, msg: "go" });
    await sm.drainAll(); sm.flushAll(); sm.closeAll();
    return lines.join("");
  }
  assert.match(await run(true), /── done · [\d.]+s ──/);
  assert.doesNotMatch(await run(false), /── done ·/);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.decorations.test.mjs`
Expected: FAIL — `Cannot find module '../src/ui/decorations.mjs'`

- [ ] **Step 3: 实现**

① `src/ui/decorations.mjs`(新建):

```js
// synod/src/ui/decorations.mjs — 多路输出的装饰行渲染(§2.2 turn 边界线 / §2.3 relay 横幅)。
// 纯函数;仅在 enabled(stdout) 为真(TTY)时被调用 → 总是着色输出。
import { color, labelColor } from "./ansi.mjs";

/** [label] ── done · Xs ────────── (dim 暗色;§2.2)。 */
export function turnBoundary(label, secs) {
  const prefix = color(labelColor(label), `[${label}]`);
  const body = color(2, `── done · ${secs}s ${"─".repeat(10)}`);
  return `${prefix} ${body}\n`;
}

/** [to] ◀─ relay from <from> (Nk chars) (§2.3 转发可视化)。 */
export function relayBanner(to, from, chars) {
  const k = chars >= 1000 ? `${(chars / 1000).toFixed(1)}k` : `${chars}`;
  const prefix = color(labelColor(to), `[${to}]`);
  const body = color(2, `◀─ relay from ${from} (${k} chars)`);
  return `${prefix} ${body}\n`;
}
```

② `src/session-manager.mjs` 的 `open()` 状态钩子(:153-158)替换为(计入 turn 起讫,只在 `useColor` 时画边界线):

```js
    let _turnStartAt = null;
    session.on("status", ({ status }) => {
      if (status === "running") {
        _turnStartAt = Date.now();
      } else if (status === "idle") {
        lineBuf.flush();
        if (useColor && _turnStartAt != null) {
          const secs = ((Date.now() - _turnStartAt) / 1000).toFixed(1);
          stdout.write(turnBoundary(label, secs));
          _turnStartAt = null;
        }
        _onIdle(label);
      }
    });
```

并在 `src/session-manager.mjs` 头部 import 追加:

```js
import { turnBoundary } from "./ui/decorations.mjs";
```

③ `src/relay.mjs` 的 `onTurnComplete`(:109-116)替换为(透传 meta;`enqueue` 回调签名兼容,旧 `(to,msg)` 解构忽略第三参):

```js
    onTurnComplete(fromLabel, turnText) {
      const targets = _graph.get(fromLabel);
      if (!targets || targets.size === 0) return;
      const msg = `[relay from ${fromLabel}]\n\n${turnText}`;
      for (const to of targets) {
        enqueue(to, msg, { from: fromLabel, chars: turnText.length });
      }
    },
```

④ `src/cli.mjs`:在交互段入口算一次门控,再让 relay 回调写横幅。

import 区(:18 后)追加:

```js
import { enabled } from "./ui/ansi.mjs";
import { relayBanner } from "./ui/decorations.mjs";
```

把 registry 创建(:351-354)替换为(回调用门控 + meta 画横幅):

```js
  const colorOn = enabled(stdout, env);
  let _smForRelay = null;
  const registry = createRelayRegistry((to, msg, meta) => {
    if (!_smForRelay) return;
    if (colorOn && meta) stdout.write(relayBanner(to, meta.from, meta.chars));
    _smForRelay.enqueue({ target: to, msg });
  });
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.decorations.test.mjs && npm test`
Expected: 全部 PASS(`test/relay-registry.test.mjs` / `test/relay.test.mjs` 的 `(to,msg)` 回调忽略 meta,不回归;Task 2 的 `ui.line-color` 第三测因边界线仅取含 `yo` 的 delta 行,不受影响)

- [ ] **Step 5: Commit**

```bash
git add src/ui/decorations.mjs src/session-manager.mjs src/relay.mjs src/cli.mjs test/ui.decorations.test.mjs
git commit -m "feat(ui): turn 边界线 + relay 转发可视化(TTY 门控,§2.2/§2.3)"
```

---

### Task 4: 提示符模式徽标 + 忙闲(§1)

**Files:**
- Create: `src/ui/prompt.mjs`
- Modify: `src/cli.mjs`(`createRepl` 用动态 `getPrompt`;`main` 传 `renderPrompt`)
- Test: `test/ui.prompt.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/ui.prompt.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { renderPrompt } from "../src/ui/prompt.mjs";
import { stripAnsi } from "../src/ui/ansi.mjs";

function fakeSm(rows, current) {
  return {
    _sessions: new Map(rows.map(([l, status]) => [l, { session: { status } }])),
    currentLabel: current,
  };
}

test("非 TTY → 恒为 '> '(不破 e2e 探测)", () => {
  const sm = fakeSm([["omp#1", "idle"]], "omp#1");
  assert.equal(renderPrompt({ sm, stdout: { isTTY: false }, env: {} }), "> ");
  assert.equal(renderPrompt({ sm, stdout: {}, env: {} }), "> ");
});

test("TTY 无会话 → synod ❯", () => {
  const sm = fakeSm([], null);
  assert.equal(stripAnsi(renderPrompt({ sm, stdout: { isTTY: true }, env: {} })), "synod ❯ ");
});

test("TTY 当前空闲 → [omp#1] ❯", () => {
  const sm = fakeSm([["omp#1", "idle"]], "omp#1");
  assert.equal(stripAnsi(renderPrompt({ sm, stdout: { isTTY: true }, env: {} })), "[omp#1] ❯ ");
});

test("TTY 当前忙 + 另有 2 个在跑 → 计数徽标(§1)", () => {
  const sm = fakeSm([["omp#1", "running"], ["omp#2", "running"], ["codex#1", "running"]], "omp#1");
  assert.equal(stripAnsi(renderPrompt({ sm, stdout: { isTTY: true }, env: {} })), "[omp#1 ⠧ 2 running] ❯ ");
});

test("TTY 但 NO_COLOR → 徽标无色仍显示", () => {
  const sm = fakeSm([["omp#1", "idle"]], "omp#1");
  const p = renderPrompt({ sm, stdout: { isTTY: true }, env: { NO_COLOR: "1" } });
  assert.equal(p, "[omp#1] ❯ ");
  assert.ok(!/\x1b/.test(p));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.prompt.test.mjs`
Expected: FAIL — `Cannot find module '../src/ui/prompt.mjs'`

- [ ] **Step 3: 实现**

① `src/ui/prompt.mjs`(新建):

```js
// synod/src/ui/prompt.mjs — 主持人模式提示符:模式徽标 + 当前会话 + 忙闲(§1)。
// 非 TTY 恒退化为 "> "(与现状一致,不破 e2e 的 "> " 探测)。徽标色:主持人=青(36)。
import { color } from "./ansi.mjs";

/** 据当前 sm 状态渲染一行提示符字符串(含尾随空格)。 */
export function renderPrompt({ sm, stdout, env = process.env }) {
  if (!stdout || !stdout.isTTY) return "> ";
  const useColor = !env.NO_COLOR;
  const paint = (s) => (useColor ? color(36, s) : s);

  const cur = sm.currentLabel;
  if (!cur) return `${paint("synod")} ❯ `;

  let running = 0;
  for (const [, info] of sm._sessions) {
    if (info.session && info.session.status === "running") running += 1;
  }
  const curRunning = sm._sessions.get(cur)?.session?.status === "running";
  const others = running - (curRunning ? 1 : 0);

  let inner = cur;
  if (curRunning) inner += " ⠧";            // 当前忙:静态忙标(不做定时器动画,§1)
  if (others > 0) inner += ` ${others} running`;
  return `${paint(`[${inner}]`)} ❯ `;
}
```

② `src/cli.mjs`:`createRepl`(:168-205)改为动态提示符。

import 区追加:

```js
import { renderPrompt } from "./ui/prompt.mjs";
```

`createRepl` 函数签名与两处 prompt 写点替换(`prompt` 字符串 → `getPrompt` 函数;并预留 `completer`/`history`/`onSubmit`,Task 8 接):

```js
function createRepl({ getPrompt, onLine, onSubmit, onClose, stdin = process.stdin, stdout = process.stdout, completer, history }) {
  const rl = readline.createInterface({
    input: stdin,
    output: stdout,
    terminal: stdin.isTTY ?? false,
    ...(completer ? { completer } : {}),
    ...(history ? { history } : {}),
  });
  // Start paused so piped input isn't consumed before sessions are ready.
  rl.pause();
  let exitRequested = false;
  let closed = false;

  rl.on("line", (line) => {
    if (exitRequested) return;
    const text = line.trim();
    if (!text) {
      stdout.write(getPrompt());
      return;
    }
    if (onSubmit) onSubmit(text);
    void onLine(text);
  });

  rl.on("close", () => {
    if (closed) return;
    closed = true;
    void onClose();
  });

  return {
    resume() { rl.resume(); },
    writePrompt() {
      if (!exitRequested && !closed) stdout.write(getPrompt());
    },
    closeRl() {
      exitRequested = true;
      rl.close();
    },
  };
}
```

③ `main()` 里 `repl = createRepl({...})`(:399)把 `prompt: "> "` 改为:

```js
    getPrompt: () => renderPrompt({ sm, stdout, env }),
```

(其余 `stdin/stdout/onLine/onClose` 不变。)

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.prompt.test.mjs && npm test`
Expected: 全部 PASS(`test/cli.integration.test.mjs` 的 captureStream 无 `isTTY` → `renderPrompt` 返回 `"> "`,提示符断言不回归)

- [ ] **Step 5: Commit**

```bash
git add src/ui/prompt.mjs src/cli.mjs test/ui.prompt.test.mjs
git commit -m "feat(ui): 提示符模式徽标 + 忙闲计数(非 TTY 退化为 '> ',§1)"
```

---

### Task 5: `/help` 分组(§3.2)+ 错误「下一步」hint(§3.3)

**Files:**
- Create: `src/ui/help.mjs`
- Modify: `src/session-manager.mjs`(导出 `NO_SESSION_HINT`;`enqueue`/`use` 的 No session 追加 hint)
- Modify: `src/repl-dispatch.mjs`(`/help` 命令;`/relay` 的 No session 追加 hint)
- Test: `test/ui.help-hint.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/ui.help-hint.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { createReplDispatch } from "../src/repl-dispatch.mjs";
import { createSessionManager, NO_SESSION_HINT } from "../src/session-manager.mjs";
import { HELP_TEXT, helpForCommand } from "../src/ui/help.mjs";

function cap() { return { buf: "", write(s) { this.buf += s; } }; }

test("/help 分组打印(会话/消息/转发/工作流/其他)", () => {
  const stdout = cap();
  const dispatch = createReplDispatch({
    sm: { _sessions: new Map() }, registry: {}, stdout, stderr: cap(), defaultAgent: "omp",
  });
  const r = dispatch("/help", { source: "human" });
  assert.equal(r.redraw, true);
  for (const head of ["会话(主持人模式)", "消息", "转发", "工作流", "其他"]) {
    assert.ok(stdout.buf.includes(head), `缺分组标题 ${head}`);
  }
  assert.ok(stdout.buf.includes("/close <label>"));
  assert.ok(stdout.buf.includes("/status"));
});

test("/help open → 单命令详情", () => {
  const stdout = cap();
  const dispatch = createReplDispatch({
    sm: { _sessions: new Map() }, registry: {}, stdout, stderr: cap(), defaultAgent: "omp",
  });
  dispatch("/help open", { source: "human" });
  assert.ok(stdout.buf.includes("/open"));
  assert.match(stdout.buf, /profile|--agent/);
});

test("helpForCommand 未知命令 → 提示 /help", () => {
  assert.match(helpForCommand("nope"), /no help for "nope"/);
  assert.ok(HELP_TEXT.includes("工作流"));
});

test("No session 错误追加下一步 hint(§3.3)", () => {
  const stderr = cap();
  const sm = createSessionManager({
    openBackend: async () => {}, stdout: cap(), stderr,
    report: {}, cwd: process.cwd(), defaults: {}, onIdle: () => {},
  });
  sm.enqueue({ target: "omp#9", msg: "x" });
  assert.ok(stderr.buf.includes('No session "omp#9"'));
  assert.ok(stderr.buf.includes(NO_SESSION_HINT.trim()));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.help-hint.test.mjs`
Expected: FAIL — `Cannot find module '../src/ui/help.mjs'`;`NO_SESSION_HINT` 未导出

- [ ] **Step 3: 实现**

① `src/ui/help.mjs`(新建,文案照 §3.2):

```js
// synod/src/ui/help.mjs — REPL /help 分组帮助(§3.2)。纯文本(命令是用户主动敲的,不着色)。

export const HELP_TEXT = [
  "会话(主持人模式)",
  "  /open [+profile] [--agent A] [--model M] [--effort E] [--write] [--mesh|--no-mesh]",
  "  /use <label>      /close <label>      /sessions",
  "消息",
  "  <text> → 当前会话      @<label> <text> → 定向      @all <text> → 广播",
  "转发",
  "  /relay a->b      /unrelay a->b      /relays",
  "工作流",
  "  /flow             列出可用 flow",
  "  /flow <name> [input]",
  "团队(阶段 2)",
  '  /team <name> "<task>"      /team status      /done',
  "其他",
  "  /status      /help [cmd]      /exit (Ctrl-D)      Ctrl-C 中断",
  "",
].join("\n");

const DETAILS = {
  open:
    "/open [+profile] [--agent A] [--model M] [--effort E] [--write] [--mesh|--no-mesh]\n" +
    "  开新会话。+profile 套用 synod.config.mjs 的 agent 档案;内联 flag 覆盖档案字段。\n" +
    "  例:/open +coder --model MiniMax-M3\n",
  use: "/use <label>\n  切换当前会话(后续无前缀消息发往它)。\n",
  close: "/close <label>\n  关闭会话并解除其全部 relay 绑定。\n",
  sessions: "/sessions\n  表格列出全部会话(当前以 * 标注;RELAY 列示出/入边)。\n",
  relay: "/relay <from>-><to>\n  把 from 每个 turn 的完整输出转发给 to。\n",
  unrelay: "/unrelay <from>-><to>\n  移除一条转发规则。\n",
  flow: "/flow [<name> [input]]\n  省略 name 列出可用 flow;带 name 运行(进度视图 + 头尾横幅)。\n",
  status: "/status\n  一行总览:会话数 / 在跑数 / 活跃 relay 数 / 当前会话 / flow。\n",
};

/** 单命令详情(允许带或不带前导 /);未知 → 提示回 /help。 */
export function helpForCommand(topic) {
  const key = String(topic).replace(/^\//, "");
  return DETAILS[key] ?? `no help for "${topic}". Try /help.\n`;
}
```

② `src/session-manager.mjs`:在 `checkAgentAvailable` 之上(:48 前)加导出常量:

```js
/** No session / No current session 错误的「下一步」建议行(§3.3)。 */
export const NO_SESSION_HINT = "  hint: /sessions 查看活跃会话;/open 新开一个\n";
```

`enqueue` 的 No session 写点(:191-193)替换为:

```js
    const info = target ? _sessions.get(target) : _sessions.get(_currentLabel);
    if (!info) {
      stderr.write(
        (target ? `No session "${target}"\n` : "No current session. Use /open to create one.\n") + NO_SESSION_HINT,
      );
      return false;
    }
```

`use` 的 No session 写点(:209)替换为:

```js
    stderr.write(`No session "${target}"\n${NO_SESSION_HINT}`);
```

模块尾导出(:292)追加 `NO_SESSION_HINT`:

```js
export { createSessionManager, createLineBuffer, checkAgentAvailable, NO_SESSION_HINT };
```

(删去上面新加的 `export const` 行里的 `export` 关键字以避免重复导出——二选一。**采用 named 行内 `export const`,模块尾导出列表不再列 `NO_SESSION_HINT`**。即:保留 ① 的 `export const NO_SESSION_HINT = ...;`,模块尾 `export {}` 维持原样不加。)

③ `src/repl-dispatch.mjs`:import 区(:32)追加并接 `/help` + `/relay` 的 hint。

import:

```js
import { HELP_TEXT, helpForCommand } from "./ui/help.mjs";
import { NO_SESSION_HINT } from "./session-manager.mjs";
```

`dispatchHumanSlash` 里 `/relay` 的两处 No session(:248、:252)各自的 `stderr.write(...)` 改为附 hint:

```js
      if (!sm._sessions.has(parsed.from)) {
        stderr.write(`No session "${parsed.from}"\n${NO_SESSION_HINT}`);
        return { redraw: true };
      }
      if (!sm._sessions.has(parsed.to)) {
        stderr.write(`No session "${parsed.to}"\n${NO_SESSION_HINT}`);
        return { redraw: true };
      }
```

`dispatchHumanSlash` 的「Unknown / command」分支(:343-345)之前插入 `/help`:

```js
    if (cmd === "/help") {
      const topic = rest[0];
      stdout.write(topic ? helpForCommand(topic) : HELP_TEXT + "\n");
      return { redraw: true };
    }
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.help-hint.test.mjs && npm test`
Expected: 全部 PASS(`test/repl-dispatch.test.mjs` 的 `No session` 断言用 `.includes`,附 hint 不回归;`test/cli.integration.test.mjs:126` 同理)

- [ ] **Step 5: Commit**

```bash
git add src/ui/help.mjs src/session-manager.mjs src/repl-dispatch.mjs test/ui.help-hint.test.mjs
git commit -m "feat(ui): /help 分组 + No session 错误附下一步 hint(§3.2/§3.3)"
```

---

### Task 6: `/close <label>`(P2-19)+ `/status` + control-wire `dropLabel`(P2-25)

**Files:**
- Modify: `src/session-manager.mjs`(新增 `close(label)`,导出)
- Modify: `src/control-wire.mjs`(`wireControl` 返回 `dropLabel`)
- Modify: `src/repl-dispatch.mjs`(`/close`、`/status`;`onCloseLabel`/`flowStatus` 依赖)
- Modify: `src/cli.mjs`(把 `dropLabel` 接进 dispatch;`flowStatus`)
- Test: `test/ui.close-status.test.mjs`;追加 `test/control-wire.test.mjs`

- [ ] **Step 1: 写失败测试**

新建 `test/ui.close-status.test.mjs`:

```js
// test/ui.close-status.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { createSessionManager } from "../src/session-manager.mjs";
import { createReplDispatch } from "../src/repl-dispatch.mjs";
import { createRelayRegistry } from "../src/relay.mjs";
import { FakeSession } from "./helpers/fake-backend.mjs";

function cap() { return { buf: "", write(s) { this.buf += s; } }; }
function makeSm() {
  return createSessionManager({
    openBackend: async () => new FakeSession({ deltas: ["x\n"] }),
    stdout: cap(), stderr: cap(),
    report: { omp: { available: true }, codex: { available: true } },
    cwd: process.cwd(), defaults: {}, onIdle: () => {},
  });
}

test("sm.close(label):关会话、移出 _sessions、重指当前会话", async () => {
  const sm = makeSm();
  const a = await sm.open({ agent: "omp" });    // omp#1
  const b = await sm.open({ agent: "codex" });  // codex#1, current
  assert.equal(sm.currentLabel, b);
  const sessA = sm._sessions.get(a).session;
  assert.equal(sm.close(b), true);
  assert.equal(sm._sessions.has(b), false);
  assert.equal(sm.currentLabel, a, "当前会话关闭后回退到剩余会话");
  assert.equal(sm.close(a), true);
  assert.equal(sessA._closed, true);
  assert.equal(sm.currentLabel, null, "最后一个关闭后无当前会话");
});

test("sm.close 未知 label → false", () => {
  assert.equal(makeSm().close("ghost#1"), false);
});

test("/close:dispatch 调 sm.close + registry.removeForLabel + onCloseLabel", () => {
  const closed = [], dropped = [];
  const sm = { _sessions: new Map([["omp#1", {}]]), currentLabel: "omp#1", close: (l) => (closed.push(l), true) };
  const registry = createRelayRegistry(() => {});
  let removedFor = null;
  registry.removeForLabel = (l) => { removedFor = l; };
  const stdout = cap();
  const dispatch = createReplDispatch({
    sm, registry, stdout, stderr: cap(), defaultAgent: "omp",
    onCloseLabel: (l) => dropped.push(l),
  });
  const r = dispatch("/close omp#1", { source: "human" });
  assert.equal(r.redraw, true);
  assert.deepEqual(closed, ["omp#1"]);
  assert.equal(removedFor, "omp#1");
  assert.deepEqual(dropped, ["omp#1"]);
  assert.ok(stdout.buf.includes("Closed omp#1"));
});

test("/close 无参 → usage", () => {
  const stderr = cap();
  const dispatch = createReplDispatch({ sm: { _sessions: new Map() }, registry: {}, stdout: cap(), stderr, defaultAgent: "omp" });
  dispatch("/close", { source: "human" });
  assert.ok(stderr.buf.includes("Usage: /close <label>"));
});

test("/close 在 agent-fence 下被拒(仅主持人可关会话)", async () => {
  const dispatch = createReplDispatch({ sm: { _sessions: new Map([["omp#1", {}]]) }, registry: {}, stdout: cap(), stderr: cap(), defaultAgent: "omp" });
  const r = await dispatch("/close omp#1", { source: "agent-fence", depth: 0 });
  assert.equal(r.ok, false);
  assert.match(r.reason, /not allowed in agent-fence/);
});

test("/status:一行总览(会话数/在跑/relay/当前/flow)", () => {
  const stdout = cap();
  const sm = {
    _sessions: new Map([
      ["omp#1", { session: { status: "running" } }],
      ["codex#1", { session: { status: "idle" } }],
    ]),
    currentLabel: "omp#1",
  };
  const registry = createRelayRegistry(() => {});
  registry.add("omp#1", "codex#1");
  const dispatch = createReplDispatch({ sm, registry, stdout, stderr: cap(), defaultAgent: "omp", flowStatus: () => "none" });
  const r = dispatch("/status", { source: "human" });
  assert.equal(r.redraw, true);
  assert.match(stdout.buf, /sessions: 2 \(1 running\)/);
  assert.match(stdout.buf, /relays: 1/);
  assert.match(stdout.buf, /current: omp#1/);
  assert.match(stdout.buf, /flow: none/);
});
```

追加到 `test/control-wire.test.mjs`(复用文件内 `fakeSm`/`fakeRegistry`/`fakeDispatch`/`captureStream`),在文件尾部 `describe("wireControl depth map", ...)` 块内或其后追加:

```js
  it("dropLabel 清 _depthMap → 该 label 再产 fence 时 depth 归 0(P2-25)", async () => {
    const sm = fakeSm({ _sessions: [["omp#1", {}], ["codex#1", {}]] });
    const registry = fakeRegistry();
    const stderr = captureStream();
    const dispatch = fakeDispatch({
      "/open --agent codex|0": { ok: true, label: "codex#1" },
      "/open --agent omp|1": { ok: true, label: "omp#2" },
      "/open --agent omp|0": { ok: true, label: "omp#3" },
    });
    const { onTurnComplete, dropLabel } = wireControl({ sm, registry, stderr, dispatch });

    // omp#1(depth0)开出 codex#1 → 记 codex#1 = depth1
    await onTurnComplete("omp#1", { text: "```synod\n/open --agent codex\n```" });
    await new Promise((r) => setImmediate(r));
    dispatch.calls.length = 0;

    // codex#1 产 fence → dispatch 收到 depth1
    await onTurnComplete("codex#1", { text: "```synod\n/open --agent omp\n```" });
    await new Promise((r) => setImmediate(r));
    assert.strictEqual(dispatch.calls[0].depth, 1, "清理前 codex#1 = depth1");

    // /close codex#1 → dropLabel 清 _depthMap
    dropLabel("codex#1");
    dispatch.calls.length = 0;
    await onTurnComplete("codex#1", { text: "```synod\n/open --agent omp\n```" });
    await new Promise((r) => setImmediate(r));
    assert.strictEqual(dispatch.calls[0].depth, 0, "dropLabel 后 codex#1 回到 depth0(P2-25)");
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.close-status.test.mjs test/control-wire.test.mjs`
Expected: FAIL — `sm.close is not a function`;`/close`/`/status` 落入「Unknown command」;`dropLabel` undefined

- [ ] **Step 3: 实现**

① `src/session-manager.mjs`:在 `closeAll`(:267)之前加 `close(label)`,并在返回对象(:278-289)里加 `close`:

```js
  /** Close one session by label: flush, kill, drop, reassign current. */
  function close(label) {
    const info = _sessions.get(label);
    if (!info) {
      stderr.write(`No session "${label}"\n${NO_SESSION_HINT}`);
      return false;
    }
    info.lineBuf.flush();
    try { info.session.close(); } catch {}
    _sessions.delete(label);
    if (_currentLabel === label) {
      const remaining = [..._sessions.keys()];
      _currentLabel = remaining.length ? remaining[remaining.length - 1] : null;
    }
    return true;
  }
```

返回对象加一行(在 `closeAll,` 后):

```js
    close,
```

② `src/control-wire.mjs`:`wireControl` 返回值(:80)加 `dropLabel`:

```js
  return {
    onTurnComplete,
    /** P2-25:会话 /close 时清其 depth 记录(label 不复用,内存收尾)。 */
    dropLabel(label) { _depthMap.delete(label); },
  };
```

③ `src/repl-dispatch.mjs`:`createReplDispatch` 解构(:143)加两个依赖(默认 no-op):

```js
export function createReplDispatch({ sm, registry, stdout, stderr, defaultAgent, guardrails, runFlow, config = { agents: {} }, onCloseLabel = () => {}, flowStatus = () => "none" }) {
```

`dispatchHumanSlash` 里 `/use` 分支(:289)之后插入 `/close` 与 `/status`:

```js
    if (cmd === "/close") {
      const target = rest[0];
      if (!target) {
        stderr.write("Usage: /close <label>\n");
        return { redraw: true };
      }
      if (sm.close(target)) {
        registry.removeForLabel(target);   // P2-19:解绑 relay
        onCloseLabel(target);              // P2-25:清 control-wire _depthMap
        stdout.write(`Closed ${target}\n`);
      }
      return { redraw: true };
    }

    if (cmd === "/status") {
      let running = 0;
      for (const [, info] of sm._sessions) {
        if (info.session && info.session.status === "running") running += 1;
      }
      const relays = registry.list ? registry.list().length : 0;
      stdout.write(
        `sessions: ${sm._sessions.size} (${running} running) · relays: ${relays} · ` +
        `current: ${sm.currentLabel ?? "(none)"} · flow: ${flowStatus()}\n`,
      );
      return { redraw: true };
    }
```

(`/close`、`/status` 不进 `dispatchAgentFence` 的白名单——agent-fence 只允许 `/open`、`@label`、`/relay`,其余落入既有 `command not allowed in agent-fence` 拒绝,无需改动。)

④ `src/cli.mjs`:把 `dropLabel` 经晚绑定接进 dispatch,并给 `flowStatus`。

`dispatch` 创建(:387-393)替换为(沿用既有两阶段晚绑定风格):

```js
  let _dropDepth = () => {};
  const dispatch = createReplDispatch({
    sm, registry, stdout, stderr,
    defaultAgent: args.agent,
    guardrails: { maxSessions: 10, maxDepth: 3, allowWrite: false },
    runFlow,
    config,
    onCloseLabel: (label) => _dropDepth(label),
    flowStatus: () => (_pendingFlows.size > 0 ? `${_pendingFlows.size} running` : "none"),
  });
```

`wireControl`(:395-398)解构出 `dropLabel` 并回填:

```js
  const { onTurnComplete: composedOnTurnComplete, dropLabel } = wireControl({
    sm, registry, stderr, dispatch,
  });
  _composedOnTurnComplete = composedOnTurnComplete;
  _dropDepth = dropLabel;
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.close-status.test.mjs test/control-wire.test.mjs && npm test`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/session-manager.mjs src/control-wire.mjs src/repl-dispatch.mjs src/cli.mjs test/ui.close-status.test.mjs test/control-wire.test.mjs
git commit -m "feat(ui): /close(解绑 relay + 清 depthMap,P2-19/P2-25)+ /status 总览"
```

---

### Task 7: `/sessions` 表格 + relay 列(§4)

**Files:**
- Create: `src/ui/sessions-table.mjs`
- Modify: `src/session-manager.mjs`(`createSessionManager` 接 `relays`;`list()` 渲染表格)
- Modify: `src/cli.mjs`(把 `() => registry.list()` 注入 sm)
- Test: `test/ui.sessions-table.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/ui.sessions-table.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { renderSessionsTable } from "../src/ui/sessions-table.mjs";
import { stripAnsi } from "../src/ui/ansi.mjs";
import { createSessionManager } from "../src/session-manager.mjs";
import { FakeSession } from "./helpers/fake-backend.mjs";

test("表格:列头齐全、* 标当前、RELAY 列出/入边、空 model → (default)", () => {
  const t = renderSessionsTable({
    sessions: [
      { label: "omp#1", agent: "omp", model: "ds-v4", status: "idle", turns: 4 },
      { label: "codex#1", agent: "codex", model: "", status: "running", turns: 3 },
    ],
    currentLabel: "omp#1",
    relays: [{ from: "omp#1", to: "codex#1" }],
  });
  assert.match(t, /LABEL\s+BACKEND\s+MODEL\s+STATE\s+TURNS\s+RELAY/);
  const lines = t.split("\n");
  const ompRow = lines.find((l) => l.includes("omp#1") && !l.includes("LABEL"));
  assert.match(ompRow, /^\s*\*\s+omp#1/, "当前会话以 * 标注");
  assert.match(ompRow, /→ codex#1/, "出边");
  const codexRow = lines.find((l) => l.includes("codex#1") && !l.includes("LABEL"));
  assert.match(codexRow, /← omp#1/, "入边");
  assert.match(codexRow, /\(default\)/, "空 model 显示 (default)");
});

test("colorOn → label 着色,strip 后等于无色表格", () => {
  const args = {
    sessions: [{ label: "omp#1", agent: "omp", model: "m", status: "idle", turns: 1 }],
    currentLabel: "omp#1", relays: [],
  };
  const plain = renderSessionsTable({ ...args, colorOn: false });
  const colored = renderSessionsTable({ ...args, colorOn: true });
  assert.ok(/\x1b\[/.test(colored));
  assert.equal(stripAnsi(colored), plain);
});

test("sm.list() 渲染表格 + relay 列(经注入的 relays())", async () => {
  const lines = [];
  const sm = createSessionManager({
    openBackend: async () => new FakeSession({}),
    stdout: { write: (s) => lines.push(s) }, stderr: { write() {} },
    report: { omp: { available: true } }, cwd: process.cwd(),
    defaults: {}, onIdle: () => {},
    relays: () => [{ from: "omp#1", to: "omp#2" }],
  });
  await sm.open({ agent: "omp" });
  await sm.open({ agent: "omp" });
  sm.list();
  const out = lines.join("");
  assert.match(out, /LABEL\s+BACKEND/);
  assert.match(out, /→ omp#2/);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.sessions-table.test.mjs`
Expected: FAIL — `Cannot find module '../src/ui/sessions-table.mjs'`

- [ ] **Step 3: 实现**

① `src/ui/sessions-table.mjs`(新建):

```js
// synod/src/ui/sessions-table.mjs — /sessions 表格(§4)。纯函数;colorOn 时着色 label。
import { color, labelColor } from "./ansi.mjs";

function pad(s, n) {
  s = String(s);
  return s.length >= n ? s : s + " ".repeat(n - s.length);
}

/**
 * @param {object} a
 * @param {Array<{label,agent,model,status,turns}>} a.sessions
 * @param {string|null} a.currentLabel
 * @param {Array<{from,to}>} [a.relays]
 * @param {boolean} [a.colorOn]
 * @returns {string} 完整表格(含首尾空行),可直接 stdout.write。
 */
export function renderSessionsTable({ sessions, currentLabel, relays = [], colorOn = false }) {
  const out = [""];
  out.push("   LABEL    BACKEND  MODEL              STATE     TURNS  RELAY");
  for (const s of sessions) {
    const marker = s.label === currentLabel ? "*" : " ";
    const edges = [
      ...relays.filter((r) => r.from === s.label).map((r) => `→ ${r.to}`),
      ...relays.filter((r) => r.to === s.label).map((r) => `← ${r.from}`),
    ].join(" ");
    const labelCell = colorOn
      ? color(labelColor(s.label), pad(s.label, 8))
      : pad(s.label, 8);
    const row =
      ` ${marker} ${labelCell} ${pad(s.agent, 7)} ${pad(s.model || "(default)", 17)} ` +
      `${pad(s.status, 8)} ${pad(String(s.turns), 5)}  ${edges}`;
    out.push(row.replace(/\s+$/, ""));
  }
  out.push("");
  return out.join("\n") + "\n";
}
```

② `src/session-manager.mjs`:`createSessionManager` 解构(:96)加 `relays`:

```js
function createSessionManager({ openBackend, stdout, stderr, report, cwd, defaults, onIdle, onTurnComplete, errorLeadingNewline = false, relays }) {
```

在 `_onTurnComplete`(:102)附近加:

```js
  const _relays = relays || (() => []);
```

`list()`(:214-224)整体替换为(渲染表格):

```js
  /** Write the sessions table to stdout (§4: * current + RELAY column). */
  function list() {
    const rows = [];
    for (const [label, info] of _sessions) {
      const sum = info.session.summary();
      rows.push({ label, agent: sum.agent, model: sum.model, status: sum.status, turns: sum.turnCount });
    }
    stdout.write(renderSessionsTable({
      sessions: rows,
      currentLabel: _currentLabel,
      relays: _relays(),
      colorOn: enabled(stdout),
    }));
  }
```

import 区(Task 2 已加 `enabled, color, labelColor`)追加:

```js
import { renderSessionsTable } from "./ui/sessions-table.mjs";
```

③ `src/cli.mjs`:`createSessionManager({...})`(:359-369)调用里加一行(把 relay 列表注入 sm.list):

```js
    relays: () => registry.list(),
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.sessions-table.test.mjs && npm test`
Expected: 全部 PASS（`test/session-manager.test.mjs:207` 的 `* codex#1` / `  omp#1` 子串断言被新表格行 ` * codex#1 ...` / `   omp#1 ...` 满足,不回归;非 TTY fake stdout → 无色）

- [ ] **Step 5: Commit**

```bash
git add src/ui/sessions-table.mjs src/session-manager.mjs src/cli.mjs test/ui.sessions-table.test.mjs
git commit -m "feat(ui): /sessions 表格 + RELAY 出入边列(§4)"
```

---

### Task 8: Tab 补全(§3.1)+ 历史持久化(§3.4)

**Files:**
- Create: `src/ui/completer.mjs`、`src/ui/history.mjs`
- Modify: `src/cli.mjs`(`createRepl` 接 `completer`/`history`/`onSubmit`;`main` 构造补全器、加载/追加历史)
- Test: `test/ui.completer.test.mjs`、`test/ui.history.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/ui.completer.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { makeCompleter } from "../src/ui/completer.mjs";

function smWith(labels) {
  return { _sessions: new Map(labels.map((l) => [l, {}])), currentLabel: labels[0] ?? null };
}

test("行首 / → 命令名候选", () => {
  const c = makeCompleter({ sm: smWith([]), config: {}, flows: [], backendNames: () => [] });
  const [hits, word] = c("/op");
  assert.ok(hits.includes("/open"));
  assert.equal(word, "/op");
});

test("/use <空> → 活跃会话 label;word 为空", () => {
  const c = makeCompleter({ sm: smWith(["omp#1", "codex#1"]), config: {}, flows: [], backendNames: () => [] });
  const [hits, word] = c("/use ");
  assert.deepEqual(hits.sort(), ["codex#1", "omp#1"]);
  assert.equal(word, "");
});

test("@ → @all + @label", () => {
  const c = makeCompleter({ sm: smWith(["omp#1"]), config: {}, flows: [], backendNames: () => [] });
  const [hits] = c("@");
  assert.ok(hits.includes("@all"));
  assert.ok(hits.includes("@omp#1"));
});

test("/open → +profile 与选项;--agent 后接 backend 名", () => {
  const c = makeCompleter({
    sm: smWith([]), config: { agents: { coder: {} } }, flows: [], backendNames: () => ["omp", "codex"],
  });
  const [h1] = c("/open ");
  assert.ok(h1.includes("+coder"));
  assert.ok(h1.includes("--agent"));
  const [h2, w2] = c("/open --agent co");
  assert.deepEqual(h2, ["codex"]);
  assert.equal(w2, "co");
});

test("/flow <部分> → flow 名", () => {
  const c = makeCompleter({ sm: smWith([]), config: {}, flows: [{ name: "qa-loop" }, { name: "hello" }], backendNames: () => [] });
  const [hits] = c("/flow h");
  assert.deepEqual(hits, ["hello"]);
});

test("/relay → from-> 候选;含 -> 后补 to", () => {
  const c = makeCompleter({ sm: smWith(["omp#1", "codex#1"]), config: {}, flows: [], backendNames: () => [] });
  const [h1] = c("/relay om");
  assert.ok(h1.includes("omp#1->"));
  const [h2] = c("/relay omp#1->co");
  assert.ok(h2.includes("omp#1->codex#1"));
});
```

```js
// test/ui.history.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { mkdtempSync, writeFileSync, readFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { loadHistory, appendHistory, historyPath } from "../src/ui/history.mjs";

test("historyPath:<home>/.synod/history", () => {
  assert.equal(historyPath("/h"), join("/h", ".synod", "history"));
});

test("loadHistory:缺文件→空;读取 newest-first 且封顶", () => {
  const f = join(mkdtempSync(join(tmpdir(), "synod-hist-")), "history");
  assert.deepEqual(loadHistory(f, 1000), []);
  writeFileSync(f, "one\ntwo\nthree\n");
  assert.deepEqual(loadHistory(f, 1000), ["three", "two", "one"]);
  assert.deepEqual(loadHistory(f, 2), ["three", "two"]);
});

test("appendHistory:追加非空行、自动建目录、忽略空白", () => {
  const f = join(mkdtempSync(join(tmpdir(), "synod-hist-")), "sub", "history");
  appendHistory(f, "hello");
  appendHistory(f, "   ");
  appendHistory(f, "world");
  assert.equal(readFileSync(f, "utf8"), "hello\nworld\n");
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.completer.test.mjs test/ui.history.test.mjs`
Expected: FAIL — 两模块均不存在

- [ ] **Step 3: 实现**

① `src/ui/completer.mjs`(新建,纯函数):

```js
// synod/src/ui/completer.mjs — readline tab 补全(§3.1)。纯函数 → 可单测。
// 返回 readline completer:completer(line) → [hits, word],word 为被补全的子串。

const COMMANDS = [
  "/open", "/use", "/close", "/sessions", "/relay", "/unrelay", "/relays",
  "/flow", "/status", "/help", "/exit", "/quit",
];
const OPEN_OPTS = ["--agent", "--model", "--effort", "--write", "--mesh", "--no-mesh"];

export function makeCompleter({ sm, config = { agents: {} }, flows = [], backendNames = () => [] }) {
  const labels = () => [...sm._sessions.keys()];

  return function completer(line) {
    // 行首命令名(尚无空格)
    if (/^\/\S*$/.test(line)) {
      const hits = COMMANDS.filter((c) => c.startsWith(line));
      return [hits.length ? hits : COMMANDS, line];
    }
    // @label / @all(尚无空格)
    if (/^@\S*$/.test(line)) {
      const cands = ["@all", ...labels().map((l) => "@" + l)];
      return [cands.filter((c) => c.startsWith(line)), line];
    }

    const parts = line.split(/\s+/);
    const cmd = parts[0];
    const endsSpace = /\s$/.test(line);
    const word = endsSpace ? "" : parts[parts.length - 1];

    if (cmd === "/use" || cmd === "/close") {
      return [labels().filter((l) => l.startsWith(word)), word];
    }

    if (cmd === "/open") {
      const prev = endsSpace ? parts[parts.length - 1] : parts[parts.length - 2];
      if (prev === "--agent") {
        return [backendNames().filter((n) => n.startsWith(word)), word];
      }
      const cands = [
        ...Object.keys(config.agents ?? {}).map((p) => "+" + p),
        ...OPEN_OPTS,
      ];
      return [cands.filter((c) => c.startsWith(word)), word];
    }

    if (cmd === "/relay" || cmd === "/unrelay") {
      const arrow = word.indexOf("->");
      if (arrow === -1) {
        return [labels().filter((l) => l.startsWith(word)).map((l) => l + "->"), word];
      }
      const left = word.slice(0, arrow + 2);
      const rightPartial = word.slice(arrow + 2);
      return [labels().filter((l) => l.startsWith(rightPartial)).map((l) => left + l), word];
    }

    if (cmd === "/flow" && parts.length <= 2) {
      return [flows.map((f) => f.name).filter((n) => n.startsWith(word)), word];
    }

    return [[], line];
  };
}
```

② `src/ui/history.mjs`(新建):

```js
// synod/src/ui/history.mjs — REPL 历史持久化(§3.4)。~/.synod/history,上限默认 1000,启动加载。
import fs from "node:fs";
import path from "node:path";

export function historyPath(home) {
  return path.join(home, ".synod", "history");
}

/** 读历史 → readline 要的 newest-first 数组(封顶 max);文件缺/坏 → []。 */
export function loadHistory(file, max = 1000) {
  try {
    const lines = fs.readFileSync(file, "utf8").split("\n").filter(Boolean);
    return lines.slice(-max).reverse();
  } catch {
    return [];
  }
}

/** 追加一行(忽略空白;自动建目录);尽力而为,出错静默。 */
export function appendHistory(file, line) {
  if (!line || !line.trim()) return;
  try {
    fs.mkdirSync(path.dirname(file), { recursive: true });
    fs.appendFileSync(file, line + "\n");
  } catch {
    /* best-effort */
  }
}
```

③ `src/cli.mjs`:`main` 构造补全器 + 历史,接进 `createRepl`。

import 区追加:

```js
import os from "node:os";
import { discoverFlows } from "./flow/loader.mjs";
import { makeCompleter } from "./ui/completer.mjs";
import { loadHistory, appendHistory, historyPath } from "./ui/history.mjs";
```

在 `flowsRoot` 定义(:377)之后、`dispatch` 创建之前插入(启动一次性发现 flow 名,best-effort):

```js
  // Tab 补全的 flow 名:启动顺扫 synod 内置 workflows(best-effort;坏 flow 整体落空,
  // 与 /flow --list 同源)。坏 flow 的 (error) 标注待 1C-a 韧性 discoverFlows 后再加。
  let _flowList = [];
  try { _flowList = await discoverFlows(flowsRoot); } catch { _flowList = []; }
  const completer = makeCompleter({ sm, config, flows: _flowList, backendNames });
  // 历史只在交互 TTY 持久化(管道/--task 无意义)。
  const _home = env.SYNOD_HOME || os.homedir();
  const _histFile = stdin.isTTY ? historyPath(_home) : null;
  const _history = _histFile ? loadHistory(_histFile) : undefined;
```

`repl = createRepl({...})`(:399)追加三个选项:

```js
    completer,
    history: _history,
    onSubmit: (text) => { if (_histFile) appendHistory(_histFile, text); },
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.completer.test.mjs test/ui.history.test.mjs && npm test`
Expected: 全部 PASS(非 TTY 集成测试 `_history` 为 undefined、`completer` 在 `terminal:false` 时被 readline 忽略,不回归)

- [ ] **Step 5: Commit**

```bash
git add src/ui/completer.mjs src/ui/history.mjs src/cli.mjs test/ui.completer.test.mjs test/ui.history.test.mjs
git commit -m "feat(ui): tab 补全(命令/label/+profile/--agent/flow/relay)+ 历史持久化(§3.1/§3.4)"
```

---

### Task 9: flow 运行视图头尾横幅 + 结果块(§5)

**Files:**
- Create: `src/ui/flow-view.mjs`
- Modify: `src/flow.mjs`(progress 开启时接入头尾横幅 + 计数 sink)
- Test: `test/ui.flow-view.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/ui.flow-view.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { createFlowView } from "../src/ui/flow-view.mjs";
import { stripAnsi } from "../src/ui/ansi.mjs";

function cap(isTTY = false) {
  let buf = "";
  return { write(s) { buf += s; }, isTTY, get out() { return buf; } };
}

test("banner + result:头尾横幅 + 结果 JSON + turns/sessions/耗时", () => {
  let t = 0;
  const stdout = cap();
  const view = createFlowView({ stdout, name: "qa-loop", clock: () => (t += 1000), env: {} });
  view.banner();
  const sink = view.countingSink({ emit() {} });
  sink.emit({ type: "opening" });
  sink.emit({ type: "start" });
  sink.emit({ type: "start" });
  view.result({ passed: true });
  const out = stripAnsi(stdout.out);
  assert.match(out, /── flow qa-loop ─+/);
  assert.match(out, /── result ─+/);
  assert.match(out, /"passed": true/);
  assert.match(out, /── done · 2 turns · 1 sessions · [\d.]+s ─+/);
});

test("countingSink 透传内层 sink", () => {
  const seen = [];
  const view = createFlowView({ stdout: cap(), name: "x", clock: () => 0, env: {} });
  const sink = view.countingSink({ emit: (e) => seen.push(e.type) });
  sink.emit({ type: "delta", text: "hi" });
  assert.deepEqual(seen, ["delta"]);
});

test("非 TTY:横幅无 ANSI(降级);result 仍打 JSON", () => {
  const stdout = cap(false);
  const view = createFlowView({ stdout, name: "x", clock: () => 0, env: {} });
  view.banner();
  view.result({ ok: 1 });
  assert.ok(!/\x1b\[/.test(stdout.out));
  assert.match(stdout.out, /"ok": 1/);
});

test("TTY:横幅着色", () => {
  const stdout = cap(true);
  const view = createFlowView({ stdout, name: "x", clock: () => 0, env: {} });
  view.banner();
  assert.ok(/\x1b\[/.test(stdout.out));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/ui.flow-view.test.mjs`
Expected: FAIL — `Cannot find module '../src/ui/flow-view.mjs'`

- [ ] **Step 3: 实现**

① `src/ui/flow-view.mjs`(新建):

```js
// synod/src/ui/flow-view.mjs — /flow 运行视图(§5):头尾横幅 + 结果块 + turns/sessions/耗时计数。
// 仅在 progress 开启时使用(REPL /flow 与 --progress)。run 目录尾行待 1C 的 per-run 目录。
import { enabled, color } from "./ansi.mjs";

function rule(label, stdout, env, width = 50) {
  const text = `── ${label} `;
  const dashes = "─".repeat(Math.max(3, width - text.length));
  const line = text + dashes;
  return (enabled(stdout, env) ? color(2, line) : line) + "\n";
}

export function createFlowView({ stdout, name, clock = () => Date.now(), env = process.env }) {
  const startedAt = clock();
  let turns = 0;
  let sessions = 0;
  return {
    banner() {
      stdout.write(rule(`flow ${name}`, stdout, env));
    },
    /** 包住已有 progress sink,顺手计 opening(会话)/start(turn)数。 */
    countingSink(inner) {
      return {
        emit(event) {
          if (event.type === "opening") sessions += 1;
          if (event.type === "start") turns += 1;
          if (inner) inner.emit(event);
        },
      };
    },
    result(value) {
      stdout.write(rule("result", stdout, env));
      if (value !== undefined) stdout.write(JSON.stringify(value, null, 2) + "\n");
      const secs = ((clock() - startedAt) / 1000).toFixed(1);
      stdout.write(rule(`done · ${turns} turns · ${sessions} sessions · ${secs}s`, stdout, env));
    },
    failed() {
      stdout.write(rule("failed", stdout, env));
    },
  };
}
```

② `src/flow.mjs`:`main()` 在 progress 开启时套上 flow-view。

import 区(:26 后)追加:

```js
import { createFlowView } from "./ui/flow-view.mjs";
```

`progressSink` 定义处(:258-259)替换为(创建 view 并用计数 sink 包住默认 sink):

```js
  const progressEnabled = args.progress || process.env.SYNOD_PROGRESS === "1";
  const baseSink = progressEnabled ? createDefaultProgressSink(stdout) : undefined;
  const view = progressEnabled ? createFlowView({ stdout, name: args.name, clock: () => Date.now() }) : null;
  const progressSink = view ? view.countingSink(baseSink) : baseSink;
```

(下游 `createRuntime({ ..., progress: progressSink })` 不变——它已收 `progressSink`。)

`flow` 解析完成后、运行前(:316-317 之间,`const ctx = runtime.createCtx(...)` 之前)插入横幅:

```js
  view?.banner();
```

成功分支(:319-323)替换为(view 接管结果块,否则维持纯 JSON):

```js
  try {
    const result = await runFlow(runtime, flow, ctx, input);
    if (view) {
      view.result(result);
    } else if (result !== undefined) {
      stdout.write(JSON.stringify(result, null, 2) + "\n");
    }
    return 0;
  } catch (err) {
    view?.failed();
    stderr.write(`Error: flow "${args.name}" failed: ${err.message}\n`);
    return 1;
  }
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/ui.flow-view.test.mjs && npm test`
Expected: 全部 PASS(`test/flow.main.test.mjs` 不带 `--progress` → `view` 为 null → 仍只打纯 JSON,不回归;`scripts/acceptance-flow.mjs` 的 FA1–FA5 不用 `--progress`,无横幅)

- [ ] **Step 5: Commit**

```bash
git add src/ui/flow-view.mjs src/flow.mjs test/ui.flow-view.test.mjs
git commit -m "feat(ui): /flow 运行视图头尾横幅 + 结果块 + turns/sessions/耗时(§5)"
```

---

### Task 10: 通知钩子 + 终端铃(§4.13)

**Files:**
- Create: `src/notify.mjs`
- Modify: `src/config.mjs`(`hooks` 段校验 + 合并)
- Modify: `src/flow.mjs`(构造 notifier;onDone/onError + BEL/标题)
- Modify: `src/flow/runtime.mjs`(`onApprovalNeeded` 透传)
- Modify: `src/flow/api/approve.mjs`(触发 `onApprovalNeeded`)
- Test: `test/notify.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// test/notify.test.mjs
import test from "node:test";
import assert from "node:assert/strict";
import { mkdtempSync, mkdirSync, writeFileSync, readFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { createNotifier } from "../src/notify.mjs";
import { loadConfig } from "../src/config.mjs";
import { createApprove } from "../src/flow/api/approve.mjs";

function cap() { return { buf: "", write(s) { this.buf += s; } }; }

test("fire:执行命令并以环境变量传 EVENT/RUN_ID/EXIT_CODE", async () => {
  const out = join(mkdtempSync(join(tmpdir(), "synod-notify-")), "out.txt");
  const cmd = `node -e "require('fs').writeFileSync(process.env.OUT,[process.env.SYNOD_EVENT,process.env.SYNOD_RUN_ID,process.env.SYNOD_EXIT_CODE].join(','))"`;
  const n = createNotifier({ config: { hooks: { onDone: cmd } }, stdout: cap(), stderr: cap(), env: { ...process.env, OUT: out } });
  await n.fire("onDone", { runId: "r1", exitCode: 0 });
  assert.equal(readFileSync(out, "utf8"), "onDone,r1,0");
});

test("fire:未配置事件 → no-op 不报错", async () => {
  const n = createNotifier({ config: { hooks: {} }, stdout: cap(), stderr: cap(), env: {} });
  await n.fire("onError", { runId: "r" });
});

test("fire:命令非零退出 → 仅警告不抛", async () => {
  const stderr = cap();
  const n = createNotifier({ config: { hooks: { onError: 'node -e "process.exit(3)"' } }, stdout: cap(), stderr, env: { ...process.env } });
  await n.fire("onError", { runId: "r" });
  assert.match(stderr.buf, /hook onError exited 3/);
});

test("bell/title:TTY 写 BEL/OSC0;非 TTY 静默(headless 不噪)", () => {
  const tty = cap(); tty.isTTY = true;
  createNotifier({ config: {}, stdout: tty, stderr: cap(), env: {} }).bell();
  createNotifier({ config: {}, stdout: tty, stderr: cap(), env: {} }).title("synod: done");
  assert.ok(tty.buf.includes("\x07"));
  assert.ok(tty.buf.includes("\x1b]0;synod: done\x07"));

  const plain = cap();
  const np = createNotifier({ config: {}, stdout: plain, stderr: cap(), env: {} });
  np.bell(); np.title("x");
  assert.equal(plain.buf, "", "非 TTY 零控制字符");
});

test("config:hooks 段校验(合法合并 / 未知键拒 / 非字符串拒)", async () => {
  const home = mkdtempSync(join(tmpdir(), "synod-h-"));
  mkdirSync(join(home, ".synod"), { recursive: true });
  const cwd = mkdtempSync(join(tmpdir(), "synod-p-"));

  writeFileSync(join(cwd, "synod.config.mjs"), `export default { hooks: { onDone: "sh notify.sh" } };`);
  assert.equal((await loadConfig({ cwd, home })).hooks.onDone, "sh notify.sh");

  writeFileSync(join(cwd, "synod.config.mjs"), `export default { hooks: { onBogus: "x" } };`);
  await assert.rejects(loadConfig({ cwd, home }), /onBogus.*known hook/s);

  writeFileSync(join(cwd, "synod.config.mjs"), `export default { hooks: { onDone: 123 } };`);
  await assert.rejects(loadConfig({ cwd, home }), /hooks\.onDone.*non-empty/s);
});

test("approve:onApprovalNeeded 在等待输入前触发一次", async () => {
  let fired = 0;
  const io = { stdout: { write() {} }, question: async () => "accept" };
  const approve = createApprove({ io, logger: { logStep: async () => {} }, onApprovalNeeded: () => { fired += 1; } });
  const r = await approve({ runId: "r1" }, { content: "review me" });
  assert.equal(r.accepted, true);
  assert.equal(fired, 1);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/notify.test.mjs`
Expected: FAIL — `Cannot find module '../src/notify.mjs'`;config 无 `hooks` 校验/合并;approve 无 `onApprovalNeeded`

- [ ] **Step 3: 实现**

① `src/notify.mjs`(新建):

```js
// synod/src/notify.mjs — config 通知钩子(§4.13)+ 终端铃/标题。
//
// fire(event, ctx):若 config.hooks[event] 配了命令,spawn(shell) 执行,以环境变量
//   SYNOD_EVENT/SYNOD_RUN_ID/SYNOD_SUMMARY/SYNOD_EXIT_CODE 传上下文;失败只警告不抛。
//   headless(非 TTY)同样触发——这是「无人值守」语义。
// bell()/title(s):仅 TTY 写 ANSI BEL(\x07)与 OSC 0 标题(\x1b]0;...\x07)。
//   Windows:BEL 全平台可响;OSC0 在 Windows Terminal/Win10+ 生效,旧 conhost 静默吞掉。
import { spawn } from "node:child_process";

const HOOK_TIMEOUT_MS = 15_000;

export function createNotifier({ config, stdout = process.stdout, stderr = process.stderr, env = process.env } = {}) {
  const hooks = config?.hooks ?? {};
  const ttyOn = Boolean(stdout && stdout.isTTY);

  function fire(event, ctx = {}) {
    const cmd = hooks[event];
    if (!cmd) return Promise.resolve();
    return new Promise((resolve) => {
      let done = false;
      const finish = () => { if (!done) { done = true; resolve(); } };
      const timer = setTimeout(finish, HOOK_TIMEOUT_MS);
      if (timer.unref) timer.unref();
      let child;
      try {
        child = spawn(cmd, {
          shell: true,
          windowsHide: true,
          stdio: "ignore",
          env: {
            ...env,
            SYNOD_EVENT: event,
            SYNOD_RUN_ID: ctx.runId ?? "",
            SYNOD_SUMMARY: ctx.summary ?? "",
            SYNOD_EXIT_CODE: ctx.exitCode != null ? String(ctx.exitCode) : "",
          },
        });
      } catch (err) {
        stderr.write(`synod: hook ${event} failed to start: ${err.message}\n`);
        clearTimeout(timer);
        return finish();
      }
      child.on("error", (err) => {
        stderr.write(`synod: hook ${event} error: ${err.message}\n`);
        clearTimeout(timer);
        finish();
      });
      child.on("close", (code) => {
        if (code !== 0) stderr.write(`synod: hook ${event} exited ${code}\n`);
        clearTimeout(timer);
        finish();
      });
    });
  }

  function bell() {
    if (ttyOn) stdout.write("\x07");
  }
  function title(s) {
    if (ttyOn) stdout.write(`\x1b]0;${s}\x07`);
  }

  return { fire, bell, title };
}
```

② `src/config.mjs`:`validateLayer`(在 backends 循环 :57 之后、函数闭合 `}` 之前)追加 hooks 校验:

```js
  const hk = cfg.hooks;
  if (hk !== undefined) {
    if (!hk || typeof hk !== "object") fail(file, "hooks must be an object");
    for (const [k, v] of Object.entries(hk)) {
      if (!["onDone", "onError", "onApprovalNeeded"].includes(k)) {
        fail(file, `hooks.${k} is not a known hook (onDone/onError/onApprovalNeeded)`);
      }
      if (typeof v !== "string" || !v) {
        fail(file, `hooks.${k} must be a non-empty command string`);
      }
    }
  }
```

`loadConfig`(:73)的 `merged` 加 `hooks: {}`,并在层循环里(:84 `Object.assign(merged.defaults, ...)` 之后)合并:

```js
  const merged = { agents: {}, backends: {}, defaults: {}, hooks: {}, sources: [] };
```
```js
    Object.assign(merged.hooks, cfg.hooks ?? {});
```

③ `src/flow/api/approve.mjs`:`createApprove` 解构(:52)加 `onApprovalNeeded`,在 `io.question` 前触发一次。

```js
export function createApprove({ io, logger, onApprovalNeeded }) {
```

`io.question` 调用(:82)之前插入(present content 之后):

```js
    if (onApprovalNeeded) {
      try { onApprovalNeeded(ctx); } catch { /* 钩子失败只警告,不阻断审批 */ }
    }
```

④ `src/flow/runtime.mjs`:`createRuntime` 解构(:110-113)加 `onApprovalNeeded`,透传给 `createApprove`(:158)。

```js
export function createRuntime({
  fs, clock, openBackend, io, progress, config, onApprovalNeeded,
  workflowsRoot, maxDepth, maxActiveSubRuns,
} = {}) {
```
```js
  const approve = createApprove({ io: resolvedIo, logger, onApprovalNeeded });
```

⑤ `src/flow.mjs`:构造 notifier 并接 onDone/onError/onApprovalNeeded。

import 区追加:

```js
import { createNotifier } from "./notify.mjs";
```

在 `createRuntime` 调用(:282-294)之前插入 notifier,并把 `onApprovalNeeded` 传进 runtime;`createRuntime({...})` 增加 `onApprovalNeeded`:

```js
  const notifier = createNotifier({ config, stdout, stderr });
```
```js
    runtime = createRuntime({
      openBackend: ob,
      workflowsRoot: root,
      clock: () => Date.now(),
      fs: realFs,
      progress: progressSink,
      config,
      onApprovalNeeded: (ctx) => {
        notifier.bell();
        notifier.fire("onApprovalNeeded", { runId: ctx.runId, summary: `flow ${args.name} awaiting approval` })
          .catch(() => {});
      },
    });
```

成功/失败分支(Task 9 已重写为含 `view`)再叠加 onDone/onError + 铃/标题:

```js
  try {
    const result = await runFlow(runtime, flow, ctx, input);
    if (view) {
      view.result(result);
    } else if (result !== undefined) {
      stdout.write(JSON.stringify(result, null, 2) + "\n");
    }
    notifier.title(`synod: ${args.name} done`);
    notifier.bell();
    await notifier.fire("onDone", { runId: ctx.runId, summary: `flow ${args.name} done`, exitCode: 0 });
    return 0;
  } catch (err) {
    view?.failed();
    stderr.write(`Error: flow "${args.name}" failed: ${err.message}\n`);
    notifier.title(`synod: ${args.name} failed`);
    notifier.bell();
    await notifier.fire("onError", { runId: ctx.runId, summary: err.message, exitCode: 1 });
    return 1;
  }
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `node --test test/notify.test.mjs && npm test`
Expected: 全部 PASS(`test/config.test.mjs` 不设 hooks → `merged.hooks` 为 `{}`,既有 `deepEqual(cfg.agents,{})` 等断言不涉及 hooks,不回归;`test/flow.approve.test.mjs` 不传 `onApprovalNeeded` → undefined,行为不变)

- [ ] **Step 5: Commit**

```bash
git add src/notify.mjs src/config.mjs src/flow/api/approve.mjs src/flow/runtime.mjs src/flow.mjs test/notify.test.mjs
git commit -m "feat(notify): config hooks(onDone/onError/onApprovalNeeded)+ 终端铃/标题(§4.13)"
```

---

### Task 11: 端到端验收 — 非 TTY 零噪音 + headless 钩子 + 主持人零回归

**Files:**
- Test: `test/cli-ui.integration.test.mjs`

- [ ] **Step 1: 写失败(集成)测试**

```js
// test/cli-ui.integration.test.mjs
// 端到端钉死两条硬约束:(1) 非 TTY REPL 零 ANSI 且提示符仍为 "> ";
// (2) headless(非 TTY)flow 运行后 onDone 钩子照常触发。零真 agent(echo-cli)。
import test from "node:test";
import assert from "node:assert/strict";
import { mkdtempSync, mkdirSync, writeFileSync, existsSync, readFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join, resolve } from "node:path";
import { fileURLToPath } from "node:url";
import { PassThrough } from "node:stream";

const FIX = resolve(fileURLToPath(import.meta.url), "..", "..", "fixtures", "backends");
function collector() { const c = []; return { write(s) { c.push(s); return true; }, text: () => c.join("") }; }

function makeProj(extra = "") {
  const home = mkdtempSync(join(tmpdir(), "synod-uihome-"));
  const proj = mkdtempSync(join(tmpdir(), "synod-uiproj-"));
  mkdirSync(join(home, ".synod"), { recursive: true });
  writeFileSync(join(proj, "synod.config.mjs"), `export default {
    backends: { echo: { type: "cli", bin: ${JSON.stringify(process.execPath)},
      args: [${JSON.stringify(join(FIX, "echo-cli.mjs"))}], promptVia: "arg" } },
    agents: { echoer: { backend: "echo" } },
    ${extra}
  };`);
  return { home, proj };
}

test("非 TTY REPL:零 ANSI 噪音 + 提示符仍为 '> '", async () => {
  const { home, proj } = makeProj();
  process.chdir(proj);
  const { main } = await import("../src/cli.mjs");
  const stdin = new PassThrough(); stdin.isTTY = false;
  const stdout = collector(); const stderr = collector();
  const done = main({ argv: ["node", "cli.mjs", "--agent", "echo"], stdin, stdout, stderr, env: { SYNOD_HOME: home } });
  stdin.write("hello world\n");
  const deadline = Date.now() + 15_000;
  while (!stdout.text().includes("echo: hello world") && Date.now() < deadline) {
    await new Promise((r) => setTimeout(r, 50));
  }
  stdin.write("/exit\n");
  const code = await done;
  assert.equal(code, 0);
  const out = stdout.text();
  assert.match(out, /\[echo#1\] echo: hello world/);
  assert.ok(!/\x1b\[/.test(out), "非 TTY stdout 必须零 ANSI 序列");
  assert.ok(out.includes("> "), "提示符保持 '> '");
});

test("headless 钩子:onDone 在非 TTY flow 运行后照常触发", async () => {
  const sentinel = join(mkdtempSync(join(tmpdir(), "synod-sent-")), "done.txt");
  const hookCmd = `node -e "require('fs').writeFileSync(${JSON.stringify(sentinel)}, process.env.SYNOD_EVENT)"`;
  const { home, proj } = makeProj(`hooks: { onDone: ${JSON.stringify(hookCmd)} },`);
  mkdirSync(join(proj, "workflows"));
  writeFileSync(join(proj, "workflows", "echo-flow.mjs"), `
import { agent } from "synod/flow";
export const meta = { description: "echo via profile" };
export async function run(ctx, input) {
  const out = await agent(ctx, { profile: "echoer", prompt: String(input ?? "ping") });
  return { out: out.trim() };
}
`);
  process.env.SYNOD_HOME = home;
  const { main: flowMain } = await import("../src/flow.mjs");
  const stdout = collector(); const stderr = collector();
  const code = await flowMain({ argv: ["echo-flow", "ping"], stdout, stderr, workflowsRoot: join(proj, "workflows"), cwd: proj });
  assert.equal(code, 0, stderr.text());
  assert.ok(existsSync(sentinel), "onDone 钩子应已执行");
  assert.equal(readFileSync(sentinel, "utf8"), "onDone");
});
```

- [ ] **Step 2: 跑测试确认失败 / 通过**

Run: `node --test test/cli-ui.integration.test.mjs`
Expected: 若 Task 1–10 已完成 → PASS;若先行编写 → 第二测 FAIL(onDone 钩子未接,sentinel 不生成),守住验收口径。

- [ ] **Step 3: 全量回归 + 主持人零回归 e2e**

Run: `npm test && npm run test:e2e && npm run test:e2e-flow && npm run test:e2e-shutdown`
Expected:
- `npm test` 全绿(基线 653 + 本计划新增约 40+ 测)。
- `npm run test:e2e` A1–A8(+E1–4)57 不回归(无 omp/codex 则自动 skip,记录 skip)——尤其 A1/A3 的 `[omp#\d+]` 探测、A6 `Switched to`、A8 relay、E4 `/sessions`/无 `[omp#2]` 全过(装饰 TTY 门控,e2e 子进程非 TTY → 零装饰)。
- `test:e2e-flow` FA1–FA5 不回归(不带 `--progress`,无横幅);`test:e2e-shutdown` S1/S2 不回归。

- [ ] **Step 4: Commit**

```bash
git add test/cli-ui.integration.test.mjs
git commit -m "test(e2e): 非 TTY 零 ANSI + headless onDone 钩子端到端(零真 agent)"
```

---

## 完成定义(整个计划的 DoD)

1. `npm test` 全绿(653 基线 + 新增);`npm run test:e2e` A1–A8(+E1–4)不回归;`test:e2e-flow` FA1–FA5、`test:e2e-shutdown` S1/S2 不回归。
2. **非 TTY 逐字节零噪音**:`node src/cli.mjs --agent omp < /dev/null`(或管道)输出无任何 `\x1b[`、提示符为 `"> "`(Task 11 集成测已钉死)。
3. **交互 TTY 实测**(手工):真 TTY 跑 `node src/cli.mjs`,验证:label 着色、turn 边界线、徽标提示符 `[omp#1] ❯`、tab 补全(`/<tab>`、`/use <tab>`、`/open +<tab>`、`/flow <tab>`)、`↑` 历史、`/help` 分组、`/sessions` 表格、`/close`、`/status`、`/flow qa-loop` 头尾横幅;`NO_COLOR=1` 时同界面无色但徽标仍在。
4. **通知钩子**:在 `synod.config.mjs` 配 `hooks.onDone`,跑一个 flow,确认命令被执行且 `SYNOD_EVENT/SYNOD_RUN_ID/SYNOD_EXIT_CODE` 注入;TTY 下听到铃、看到窗口标题置字。
5. **Windows 实测一轮**(看板「阶段 1 总验收」,用户执行):Windows Terminal 下着色/铃/标题正常;非 TTY 管道零序列。
6. 看板回填:`docs/V1.md` 阶段 1D「执行 1D」勾选 + commit 哈希;架构文档 §2.3 P2-19/P2-25 行标「已修」、§5 路线图 1D 段标「已落地」。

## 风险与回退

| 风险 | 缓解 |
|---|---|
| 装饰行(边界线/relay 横幅)在非 TTY 改变输出 → e2e 回归 | **全部 TTY 门控**(`enabled(stdout)`);e2e 子进程非 TTY → 零装饰;Task 2/3/11 各自断言「非 TTY 零 ANSI」。设计 §2.2「非 TTY 纯文本一行」被有意推翻(见 Task 3 门控决定 + Self-Review)。 |
| 着色破坏 acceptance 的 `[omp#\d+]` / `* codex#1` / `  omp#1` 子串探测 | 着色只在 TTY 包裹 `[label]`,strip 后逐字节同旧;acceptance 子进程非 TTY → 无色;表格行 ` * codex#1 ` / `   omp#1 ` 含旧子串(Task 7 已核)。 |
| 动态提示符在 TTY + manual-write 模式与 readline 光标冲突 | 沿用现有「手动 `stdout.write(prompt)`」既成模式,仅把固定串换成函数;非 TTY 行为不变。若 1C-a 接管 readline,提示符渲染同形迁移。 |
| 启动顺扫 `discoverFlows` 执行所有内置 flow 顶层副作用 | 仅 synod 内置 `workflows/`(非项目目录);best-effort try/catch;与既有 `/flow --list` 同源,一次性。坏 flow → 补全落空,不崩。 |
| 通知钩子是任意命令(任意代码) | 与 flow/config 同信任模型(本机用户自写,§4.9);`stdio:"ignore"` + 15s 超时 + 失败只警告,绝不阻断主流程。 |
| `onDone`/`onError` 在进程退出前需等钩子启动 | `fire()` 返回 Promise 解析于子进程 `close`/`error`/15s 超时;flowMain `await` 之,确保钩子真正跑完再返回退出码(慢钩子最多拖 15s)。 |
| relay enqueue 回调加第三参 `meta` 破坏既有调用方 | meta 为附加位置参数;既有 `(to,msg)` 解构忽略之;`relay-registry.test`/`relay.test` 不断言回调 arity。 |

## Self-Review 记录

**Spec 覆盖(CLI_UI_DESIGN v1 项 → Task 映射):**

| 设计项(v1) | 出处 | Task |
|---|---|---|
| ansi 工具(color/enabled/labelColor,NO_COLOR/非 TTY 门控) | §8.1、§0.1/0.2 | **T1** |
| per-label 着色多路输出 | §2.1、§8.4 | **T2** |
| turn 边界线 | §2.2 | **T3** |
| 转发可视化(`◀─ relay from X`) | §2.3 | **T3** |
| 提示符模式徽标 + 忙闲 | §1 | **T4** |
| `/help` 分组 | §3.2 | **T5** |
| 错误提示带「下一步」hint | §3.3 | **T5** |
| `/close <label>`(+ relay 解绑) | §3.5、P2-19 | **T6** |
| control-wire `_depthMap` 随 /close 清理 | P2-25 | **T6** |
| `/status` 一行总览 | §3.5 | **T6** |
| `/sessions` 表格 + relay 列 | §4 | **T7** |
| Tab 补全(命令/label/+profile/--agent/flow/relay) | §3.1 | **T8** |
| 历史持久化(~/.synod/history,封顶 1000,启动加载) | §3.4 | **T8** |
| flow 运行视图头尾横幅 + 结果块 | §5 | **T9** |
| 端到端非 TTY 零噪音 + 零回归守门 | §8.5、硬约束 1/2 | **T11** |

**§4.13 通知钩子项 → Task 映射:**

| §4.13 项 | Task |
|---|---|
| config `hooks` 段 + 校验 | **T10**(config.mjs validateLayer + 合并) |
| `onDone`/`onError` 命令钩子 | **T10**(flowMain 成功/失败分支) |
| `onApprovalNeeded` 命令钩子 | **T10**(approve.mjs 触发点 + runtime 透传) |
| env 传上下文(SYNOD_EVENT/RUN_ID/SUMMARY/EXIT_CODE) | **T10**(notify.fire) |
| 钩子失败仅警告不阻断 | **T10**(stdio ignore + 超时 + warn) |
| TTY 默认 BEL + OSC 0 标题 | **T10**(notify.bell/title) |
| headless 同样触发钩子(铃仅 TTY) | **T10** + **T11**(headless onDone 端到端) |

**占位符扫描:** 全文无 TBD/「适当处理」/「类似 Task N」/「补充测试」。唯一条件步骤(T11 Step 2)给出确切失败模式(sentinel 未生成)与口径。每个改码 Step 给完整可粘贴代码,每个命令 Step 给确切命令 + 预期。

**前后 Task 符号一致性(已核):**
- `enabled/color/labelColor/stripAnsi/PALETTE_CODES`(T1 定义)→ T2(line-buffer 着色)、T3(decorations)、T4(prompt)、T7(table)、T9(flow-view)使用。
- `createLineBuffer(label, stdout, {colorize})`(T2)→ session-manager open() 调用形一致。
- `turnBoundary/relayBanner`(T3 decorations)→ session-manager / cli relay 回调。`relay.onTurnComplete` 三参 `enqueue(to,msg,{from,chars})`(T3)→ cli 回调 `(to,msg,meta)` 对齐。
- `renderPrompt({sm,stdout,env})`(T4)→ cli `getPrompt`;`createRepl` 的 `getPrompt/onSubmit/completer/history` 选项在 T4(getPrompt)与 T8(其余)分两次增量加入,签名前后一致。
- `NO_SESSION_HINT`(T5,session-manager 导出)→ repl-dispatch /relay/(T6)/close 复用。
- `sm.close(label)`(T6)+ `registry.removeForLabel`(基线)+ `wireControl.dropLabel`(T6)三件套在 repl-dispatch `/close` 串起;`onCloseLabel`/`flowStatus` 依赖 T6 定义、cli 晚绑定 `dropLabel`/`_pendingFlows` 注入。
- `renderSessionsTable({sessions,currentLabel,relays,colorOn})`(T7)← sm.list() 字段 `{label,agent,model,status,turns}` 取自 `session.summary()`(agent/model/status/turnCount,已核)。
- `makeCompleter({sm,config,flows,backendNames})`(T8)← cli 注入 `_flowList`(discoverFlows)+ `backendNames`(registry)。
- `createFlowView({stdout,name,clock,env}).{banner,countingSink,result,failed}`(T9)← flowMain 在 `progressEnabled` 时包 `createDefaultProgressSink`;progress 事件 `opening/start`(已核 agent.mjs:118 emit opening、flow.progress.test 证 start)→ sessions/turns 计数。
- `createNotifier({config,stdout,stderr,env}).{fire,bell,title}`(T10)← flowMain onDone/onError;`onApprovalNeeded`(T10)经 createRuntime → createApprove 触发点,签名 `onApprovalNeeded(ctx)` 一致。

**有意偏离设计(记录在案):**
1. **§2.2/§2.3 的「非 TTY:纯文本一行」被推翻**:turn 边界线与 relay 横幅一律 TTY 门控,非 TTY 不输出。依据 §8.5「acceptance 不受影响」+ 硬约束 1。
2. **§5 flow 尾行省略 `log: ~/.synod/runs/<runId>/`**:依赖 1C 的 per-run 目录;本计划尾行只给 `N turns · M sessions · Ts`。
3. **§3.1 `/flow` 坏 flow 的 `(error)` 标注**未做:依赖 1C-a 的韧性 `discoverFlows`;本计划补全仅列有效 flow 名。
4. **`NO_COLOR` 在 §9 列阶段 3,但本计划即生效**(它是 `enabled()` 地基);仅 `--no-color` flag、`--json`、退出码进 help 推迟阶段 3。
